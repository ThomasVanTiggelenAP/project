<!DOCTYPE html>
<html lang="nl-BE">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="cbg-primary">
    <header class="p-3 cbg-secondary">
        <article class="d-flex flex-wrap align-items-center justify-content-between">
            <img class="align-self-start" height="75px" src="/assets/images/fictieflogo.png">
            <% if (typeof user !== 'undefined' && user) { %>
                <p class="ctext-primary mt-3 display-6">Welcome to Pingponkk <%= user.userName %></p>
            <% } %>
           <ul class="nav">
            <li>
                <a href="/" class="nav-link px-2 ctext-primary">Home</a>
            </li>
            <li>
                <a href="/characteristics" class="nav-link px-2 ctext-primary">Characteristics</a>
            </li>
            <% if (typeof user !== 'undefined' && user) { %>
                <li>
                    <a href="/logout" class="nav-link px-2 ctext-primary">Logout</a>
                </li>
            <% } %>
           </ul>
       </article>
        </section>
      </header>